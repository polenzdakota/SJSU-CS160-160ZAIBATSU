magicDB_edited.sql --> is the edited version of the 1st version of the SQL code
loadingData.sql --> queries to load in text files
test.sql --> queries to run against the Database

QueryOutpu1.txt --> is the result of returning all the cards in a user's collection
QueryOutput2.txt --> is the result of counting how many CARDS a deck contains does a user have in the deck table. Using aggregation